export interface ClaimMeta {
  id: string;
  text: string;
}
export interface ArgumentMeta {
  id: string;
  text: string;
  author: string;
  isAgainst: boolean;
}

export interface CounterMeta {
  id: string;
  text: string;
  author: string;
}
